import { IconPrefix, IconName } from '@fortawesome/fontawesome-svg-core';

export interface Social {
	family: IconPrefix; // Use the correct type for IconPrefix
	icon: IconName; // Use the correct type for IconName
	link: string;
}
